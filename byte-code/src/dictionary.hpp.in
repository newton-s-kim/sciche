#pragma once

#include "common.hpp"

#include "log.hpp"

#include <cstddef>
#include <cstdint>

#define MEMBER_DICTIONARY_INIT_OFFSET @__init_pos__@
#define MEMBER_DICTIONARY_SIZE @__size__@

namespace sce {
class Dictionary {
private:
    inline int compare(const char* name, size_t ln, const char* reference, size_t lr);

public:
    bool identify(const char* name, size_t length, uint16_t* address);
    const char* get(size_t index);
};

int Dictionary::compare(const char* name, size_t ln, const char* reference, size_t lr)
{
    size_t el = (ln < lr) ? ln : lr;
    const char *a = name, *b = reference;
    size_t idx = 0;
    while (*a && *b && idx < el) {
        if (*a < *b)
            return -1;
        if (*a > *b)
            return 1;
        a++;
        b++;
        idx++;
    }
    if (ln < lr)
        return -1;
    if (ln > lr)
        return 1;
    return 0;
}

} // namespace sce
